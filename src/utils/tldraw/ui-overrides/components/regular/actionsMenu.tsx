import {
    DefaultActionsMenu,
    DefaultActionsMenuContent,
} from '@tldraw/tldraw';

export const RegularActionsMenu = () => {
    return (
        <DefaultActionsMenu>
            <DefaultActionsMenuContent />
        </DefaultActionsMenu>
    );
};