import {
    DefaultActionsMenu,
    DefaultActionsMenuContent,
} from '@tldraw/tldraw';

export const PresentationActionsMenu = () => {
    return (
        <DefaultActionsMenu>
            <DefaultActionsMenuContent />
        </DefaultActionsMenu>
    );
};