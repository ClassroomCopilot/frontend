import { DefaultColorStyle } from '@tldraw/tldraw'

// Set the default color for shapes
DefaultColorStyle.setDefaultValue('light-blue')

// Export other style configurations as needed
export const shapeStyles = {
  defaultColor: DefaultColorStyle
}